# Models

## champion_model.pkl

Generate:
```bash
python models/install_champion.py
# or
python scripts/build_neutral_champion.py
```

Version: `DEV_NEUTRAL_0.5_2026.1.0` — neutral probabilities only (no ML trade signals).
Replace with a trained model for production LIVE ML.
